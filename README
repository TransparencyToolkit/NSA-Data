This is a set of data for the NSA documents API. It includes all the documents, metadata on all the documents, and all the lists of terms extracted from the documents to generate the metadata (codewords, program names, intelligence agency names, etc.)
