This field is searchable for MB information only, although it is retrievable (in both views) for FARNDALE anc RECOVERY information as well.

The file type as defined in headers of intercept.

Fingerprint identification derived using appid

The Forward Instruction Table that identified how to route the associated data.

A code derived from Forward Triqraph representing a political state or nation, or its territory.

ECHELON routing token applied By dictionary based text keyword scanning engines so that selected intercepts can be routed to a requesting system and/or analyst(s).
Composition of Forward_Dictionary_Category and the forward selector to provide relationships on display. The forward selector is the component of the ECHELON selection expre
3-character string, determined by the collection system, and indicating the target from which the traffic originated (ie. Target country digraph -i- service letter, eg d -diplomati
from_post is from the ‘"'pre-email days"": it indicates the originating post, which might be an embassy. These fields are likely to contain a tetragraph such as BAGD (Baghdad)
The dictionary instances used by the front end system (on site) to perform dictionary scanning.